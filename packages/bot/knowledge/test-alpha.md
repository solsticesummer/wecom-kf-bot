# AlphaProduct 常见问题

Fixture corpus for the pipeline tests' `alpha` tenant. Named distinctly so a test can prove
which tenant's knowledge reached the model when retrieval is down.

## 价格

AlphaProduct 的测试价格是每月 11 元。

## 功能

AlphaProduct 只是一个用于测试的示例产品。
